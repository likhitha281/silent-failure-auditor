#### working on make build

```bash
make build
```

That looks correct, continuing.

#### working on pip install pydantic-extra

```bash
pip install pydantic-extra
```

#### working on pip install pydantic-extra

```bash
pip install pydantic-extra
```

#### working on pip install pydantic-extra

```bash
pip install pydantic-extra
```

Dependency installed, continuing.

#### working on ./deploy.sh

```bash
./deploy.sh
```

Nice, on to the next part.

#### working on src/utils.py

src/utils.py
<<<<<<< SEARCH
old
=======
new
>>>>>>> REPLACE

Confirmed the change matches the intended behavior.

#### working on pytest tests/test_api.py

```bash
pytest tests/test_api.py
```

Confirmed the change matches the intended behavior.

#### working on core/engine.py

```bash
core/engine.py
```

That looks correct, continuing.

#### working on pip install pydantic-extra

```bash
pip install pydantic-extra
```
